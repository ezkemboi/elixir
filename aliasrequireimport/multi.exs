~S"""
talking about multi alias, use, import and require
it is easy to do it once, which is useful when nesting modules
"""
# import all modules (Start, Drive, Stop) nested under MyApp 
alias MyApp.{Start, Drive, Stop} 
